# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES - AWS RDS CONFIGURATION
# =============================================================================

# DATABASE CONNECTION (AWS RDS FREE TIER)
# Optimizado para 10-12 tiendas concurrentes
# connection_limit=8    → Máximo 8 conexiones por instancia Next.js
# pool_timeout=20       → Espera 20s antes de timeout
# connect_timeout=10    → Timeout de conexión inicial a 10s

DATABASE_URL="postgresql://marketadmin:YOUR_PASSWORD@your-rds-endpoint.rds.amazonaws.com:5432/market_pos?connection_limit=8&pool_timeout=20&connect_timeout=10&schema=public"

# =============================================================================
# OPCIÓN ALTERNATIVA: Con PgBouncer (Recomendado para 15+ tiendas)
# =============================================================================
# Si usas PgBouncer, usa esta URL en lugar de la anterior:
# DATABASE_URL="postgresql://user:pass@pgbouncer-host:6432/market_pos?pgbouncer=true&connection_limit=20"

# =============================================================================
# CLOUDINARY (Para imágenes de productos)
# =============================================================================
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your_cloud_name"
CLOUDINARY_API_KEY="your_api_key"
CLOUDINARY_API_SECRET="your_api_secret"

# =============================================================================
# SUNAT (Para facturación electrónica)
# =============================================================================
SUNAT_API_URL="https://api.sunat.gob.pe"
SUNAT_CERTIFICATE_PATH="/path/to/certificate.pfx"
SUNAT_CERTIFICATE_PASSWORD="your_cert_password"

# =============================================================================
# SESSION CONFIGURATION
# =============================================================================
SESSION_SECRET="your-very-long-random-secret-key-here-at-least-32-characters"

# =============================================================================
# APLICACIÓN
# =============================================================================
NEXT_PUBLIC_APP_URL="https://your-domain.com"
NODE_ENV="production"

# =============================================================================
# MONITORING (Opcional)
# =============================================================================
# Prisma Pulse (Monitoreo en tiempo real - $19/mes)
# PULSE_API_KEY="your_pulse_api_key"

# Sentry (Error tracking - Gratis hasta 5k errores/mes)
# NEXT_PUBLIC_SENTRY_DSN="your_sentry_dsn"

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 1. NUNCA commitear este archivo con valores reales
# 2. Usar variables de entorno en tu hosting (Vercel, Railway, etc.)
# 3. Rotar credenciales cada 3-6 meses
# 4. Usar AWS Secrets Manager para credenciales sensibles en producción
