# Database connection string
# Local development (sin pooling para desarrollo)
DATABASE_URL="postgresql://market_user:market_pass@localhost:5432/market_pos_dev"
# DIRECT_URL es igual a DATABASE_URL en desarrollo (sin PgBouncer)
DIRECT_URL="postgresql://market_user:market_pass@localhost:5432/market_pos_dev"

# Production - AWS RDS Free Tier (optimizado para 10+ tiendas)
# DATABASE_URL="postgresql://user:password@your-db.region.rds.amazonaws.com:5432/market_pos?connection_limit=10&pool_timeout=20&connect_timeout=10"
# DIRECT_URL igual a DATABASE_URL si no usas PgBouncer

# Production con PgBouncer (20+ tiendas)
# DATABASE_URL="postgresql://user:pass@pgbouncer:6432/db?pgbouncer=true"
# DIRECT_URL="postgresql://user:pass@rds-host:5432/db"  # Conexión directa para migraciones

# Production - Railway/Render/Neon (ajustar según proveedor)
# DATABASE_URL="postgresql://user:password@host:5432/database?connection_limit=8&pool_timeout=15"

# Session secret for iron-session (generate a random 32+ character string)
# IMPORTANT: Change this in production!
SESSION_SECRET="your-secret-key-here-minimum-32-characters-change-in-production"

# Environment
NODE_ENV="development"

# SuperAdmin (comma-separated emails)
SUPERADMIN_EMAILS="admin@example.com,superadmin@example.com"

# ✅ MÓDULO 18.1: Cloudinary (para subir imágenes de productos)
# Obtén estas credenciales en: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME="your_cloud_name"
CLOUDINARY_API_KEY="your_api_key"
CLOUDINARY_API_SECRET="your_api_secret"
CLOUDINARY_FOLDER="market-pos-products"  # Carpeta donde se guardarán las imágenes
