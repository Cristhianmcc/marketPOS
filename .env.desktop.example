# ============================================================================
# MarketPOS Desktop - Configuración Local
# ============================================================================
# Este archivo es para desarrollo/prueba del modo desktop con PostgreSQL local.
# NO commitear .env.desktop real a git (solo este .example).
#
# Copiar este archivo a .env.desktop y ajustar valores.
# ============================================================================

# ============================================================================
# BASE DE DATOS LOCAL
# ============================================================================
# PostgreSQL local instalado en tu máquina
# Usuario/contraseña por defecto de PostgreSQL en Windows
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/marketpos_desktop"
DIRECT_URL="postgresql://postgres:postgres@127.0.0.1:5432/marketpos_desktop"

# ============================================================================
# SESIÓN
# ============================================================================
# Secreto para iron-session (mínimo 32 caracteres)
# Generar uno nuevo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="desktop-secret-key-change-this-in-production-minimum-32-chars"

# ============================================================================
# ENTORNO
# ============================================================================
NODE_ENV="production"

# Indica que estamos en modo desktop (offline)
DESKTOP_MODE="1"

# ============================================================================
# FEATURE FLAGS
# ============================================================================
# TTL en segundos para cache de feature flags (más alto = menos queries)
FEATURE_FLAGS_TTL_SECONDS="300"

# ============================================================================
# SUPERADMIN LOCAL
# ============================================================================
# Email del superadmin para pruebas locales
SUPERADMIN_EMAILS="admin@localhost"

# ============================================================================
# CLOUDINARY (OPCIONAL EN MODO OFFLINE)
# ============================================================================
# Si no tienes internet, las imágenes no se subirán a la nube.
# Puedes dejar estos valores vacíos o usar los de producción cuando
# sincronices con la nube.
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
CLOUDINARY_FOLDER="market-pos-desktop"

# ============================================================================
# SUNAT (OPCIONAL)
# ============================================================================
# Para facturación electrónica. Dejar vacío si no se usa.
SUNAT_USER=""
SUNAT_PASSWORD=""
SUNAT_CERT_PATH=""

# ============================================================================
# NOTAS
# ============================================================================
# 1. PostgreSQL debe estar instalado y corriendo en localhost:5432
# 2. Crear la base de datos antes de iniciar:
#    - Windows: scripts/desktop/db-init.ps1
#    - Linux/Mac: scripts/desktop/db-init.sh
# 3. El modo desktop funciona completamente offline
# 4. Para sincronizar con la nube, usa el módulo D5 (futuro)
